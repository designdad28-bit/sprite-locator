{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git -C \"${CLAUDE_PROJECT_DIR:-.}\" pull --ff-only --quiet 2>&1 || echo 'Note: git pull --ff-only could not fast-forward (local work or a diverged branch). Nothing was changed; pull or merge manually.'",
            "timeout": 30,
            "statusMessage": "Pulling latest from GitHub..."
          }
        ]
      }
    ]
  }
}
